package com.wayfinder.app

import android.app.Application

/** Application entry. Kept minimal for M1; future global singletons (location, SLM
 *  context) can live here. */
class WayfinderApp : Application()
