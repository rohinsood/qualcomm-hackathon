<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Classic Bluetooth (SPP) to the Arduino. Legacy permissions for Android <= 11,
         runtime BLUETOOTH_CONNECT for Android 12+. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- BLE scan for the qhackcane "Distance Watch" peripheral (Android 12+;
         older versions scan with the location permission we already hold). -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />

    <!-- "Stop" alert buzzed on the phone while the cane sees an obstacle. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Camera obstacle scan (the SCAN toggle): on-device YOLO detection. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Push-to-talk companion. Recognition runs on-device (API 31+
         on-device recognizer); audio never leaves the phone. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-feature
        android:name="android.hardware.sensor.compass"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QhackGPS">

        <!-- Vendor native libraries the QNN backends dlopen. Without these
             declarations the Android linker blocks them (API 31+ namespace
             rules): libcdsprpc = FastRPC to the Hexagon DSP/NPU (HTP),
             libOpenCL = Adreno GPU backend. -->
        <uses-native-library
            android:name="libcdsprpc.so"
            android:required="false" />
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            tools:ignore="LockedOrientationActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.QhackGPS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>